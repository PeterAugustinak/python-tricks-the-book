# standard using of list, dict, set
l = ['a', 'b', 'c']
d = {'a': 1, 'b': 2, 'c': 3}
s = {1, 2, 3}

# for better git diff readability, it is better to write every item in new
# line plus letting the comma always also after the last item. Then when
# editing (adding new item into the array), only one line is added and no
# line is edited

lc = [
    'a',
    'b',
    'c',
]

dc = {
    'a': 1,
    'b': 2,
    'c': 3,
}

sc = {
    1,
    2,
    3,
}
